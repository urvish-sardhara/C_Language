#include<stdio.h>
#include<conio.h>
main()
{
int HRA;
int DA;
int TA;
int gross_salary;
clrscr();

printf("Enter a value of base_salary :");
scanf("%d",&base_salary);

printf("Enter the value of HRA :");
scanf("%d",&HRA);

printf("Enter the value of DA :");
scanf("%d",&DA);

printf("Enter the value of TA :");
scanf("%d",&TA);

gross_salary=HRA=DA=TA=base_salary;

printf("the value of gross_salary is :%d",gross_salary);
gross_salary=20%

printf("the new value of gross_salary is :%d",gross_salary);
getch();
}
